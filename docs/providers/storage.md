# Storage — LOCAL MOCK / S3 REQUIRES CREDENTIALS
Local storage remains private and validates MIME/size. Production S3-compatible access requires endpoint, bucket and credentials; documents are accessed only with authorized short-lived signed URLs.
