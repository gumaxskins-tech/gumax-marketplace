# KYC

KYC is level based. Documents are private-object-storage references (`storageKey`) rather than sensitive database blobs. Reviews retain their decision and reviewer; access requires server-side authorization.
