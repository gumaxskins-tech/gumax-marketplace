# Providers
Provider selection is configuration-driven. Mock adapters support development. Real adapters require explicit credentials and documented official interfaces; provider failures are normalized and never expose secrets.
