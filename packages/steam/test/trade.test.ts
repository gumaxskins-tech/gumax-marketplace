import assert from "node:assert/strict";import test from "node:test";import {MockSteamProvider,TradeEngine} from "../src/index.ts";test("trade quarantine release duplicate assets events and reserve",async()=>{const e=new TradeEngine();e.addItem({id:"i",assetId:"123",classId:"c",ownerId:"u",status:"AVAILABLE"});assert.throws(()=>e.addItem({id:"x",assetId:"123",classId:"c",ownerId:"u",status:"AVAILABLE"}));e.reserve("i");assert.throws(()=>e.reserve("i"));e.create("t",["i"]);e.event("t","event","RECEIVED");e.event("t","event","RECEIVED");assert.equal(e.getItem("i")?.status,"QUARANTINED");assert.rejects(e.release("t",new MockSteamProvider(),"REVIEW",true));assert.equal((await e.release("t",new MockSteamProvider(),"ALLOW",true)).status,"RELEASED");assert.equal(e.getItem("i")?.status,"AVAILABLE")});
