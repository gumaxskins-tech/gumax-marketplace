import assert from "node:assert/strict";import test from "node:test";import {CommercialOperations} from "../src/operations.ts";test("idempotent commercial reservations prevent double buy and retain pricing snapshot",()=>{const s=new CommercialOperations();const x={id:"b",kind:"BUY" as const,userId:"u",status:"CREATED" as const,pricingSnapshotId:"ps",idempotencyKey:"k",inventoryIds:["i"],targetBudget:"10"};assert.equal(s.create(x,"ALLOW").status,"RESERVED");assert.equal(s.create(x,"ALLOW").pricingSnapshotId,"ps");assert.throws(()=>s.create({...x,id:"b2",idempotencyKey:"k2"},"ALLOW"));assert.equal(s.create({...x,id:"x",idempotencyKey:"x"},"BLOCK").status,"BLOCKED");assert.equal(s.create({...x,id:"r",idempotencyKey:"r"},"REVIEW").status,"REVIEW")});
