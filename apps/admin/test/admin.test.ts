import assert from "node:assert/strict";import test from "node:test";import {AdminService} from "../src/index.ts";test("rbac versioning and audited overrides",()=>{const s=new AdminService();assert.throws(()=>s.createConfig("SUPPORT","pricing",{},"u",new Date()));const a=s.createConfig("ADMIN","pricing",{markup:"0.1"},"a",new Date());assert.equal(s.activate("ADMIN",a.id,"a","approved").status,"ACTIVE");assert.throws(()=>s.override("SUPPORT","s","payment","x"));s.override("ADMIN","a","payment","manual review");assert.equal(s.audit.length,3)});
