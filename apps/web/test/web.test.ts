import assert from "node:assert/strict";import test from "node:test";import {MarketplaceView} from "../src/index.ts";test("web does not submit authoritative financial values",()=>{const w=new MarketplaceView();w.add({listingId:"i",source:"INVENTORY",quantity:1});assert.deepEqual(w.checkoutPayload(),{items:[{listingId:"i",source:"INVENTORY"}]});assert.throws(()=>w.add({listingId:"x",source:"ORDER",quantity:2}))});
